{
  "name": "@modal-commander/builtins",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    "./main": "./dist/main.js",
    "./renderer": "./dist/renderer.js"
  },
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "build:native": "node-gyp configure build && npm run copy-binary",
    "copy-binary": "mkdir -p dist && cp build/Release/LockCommand.node dist/"
  },
  "dependencies": {
    "node-addon-api": "^8.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.0.0"
  }
}
